# 2026-09-15 介面回歸與有限猴子測試

使用 Chrome 內部預覽，匯入既有 624 格分析紀錄，再附加指紋相同的 IMG_3289.mov；未重新宣稱身分或姿態真值。

通過：
- 未載入原片時顯示原因，品質面板回看按鈕停用。
- 回看位置衝突影格，影片時間 1.218333 秒，捲動完成後影片頂部位於視窗內（195 px）。
- 雙擊同一回看按鈕仍停在對應時間，未出現介面錯誤。
- 0 秒關閉骨架、開啟伸膝參考線，截圖確認紫色線仍存在。
- 8 次有記錄的偽隨機骨架／參考線切換，逐次核對 aria-checked 全部符合預期。
- Judge View 停用參考線控制，回動作分析恢復；報告／工作台／指南／工作台四次導航正常。
- 136 項核心測試通過，新增 SHA256 區塊邊界及缺少 secure-context API 時的 UUID／指紋一致性測試。

發現並修正：回看按鈕原本僅 seek 未捲回；參考線預設關閉且被骨架總開關遮住；HTTP 預覽缺少 randomUUID/subtle 導致紀錄匯入或原片附加失敗。相容流程仍使用 CSPRNG UUID 與標準 SHA256，不省略原片指紋比較。

限制：第一輪較長隨機操作腳本逾時，未納入通過次數；後續分批有記錄的操作才計入。瀏覽器擴充套件有 metadata 日誌錯誤，最後頁面無錯誤提示。測試环境未登入，雲端儲存未驗證；未做手機 Safari、所有影片編碼或長時間模型運算壓力測試。此為有界介面測試，不代表全部功能零缺陷或姿態判讀正確。
